import {
  formatDateJs
} from 'tweetr/helpers/format-date.js';

module('FormatDate.JsHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = formatDateJs(42);
  ok(result);
});
